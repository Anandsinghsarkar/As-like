# OmniRoute or any OpenAI-compatible gateway URL.
AI_GATEWAY_BASE_URL=http://localhost:20128

# Keep this server-side. Leave blank only when your gateway does not require authentication.
AI_GATEWAY_API_KEY=

# Model ID used when the UI's Smart Router option is selected.
AI_ROUTER_MODEL=auto
